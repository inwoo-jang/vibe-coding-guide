# 바이브코딩 가이드 — 작업 컨텍스트

코딩 초보를 위한 바이브코딩 가이드 웹. 친구 2~3명에게 배포할 목적.

## 이미 내린 결정 (다시 논의하지 말 것)

- **스택: React + Vite + Supabase, 배포는 Vercel.**
  Vue/바닐라를 검토했지만 로그인·진도저장·어드민이 들어가면서 앱이 됐고,
  AI 코딩 도구가 가장 정확한 코드를 뽑는 React로 확정했다.
- **DB는 Supabase (Firebase 아님).** 가이드의 "DB 설계 / RLS 보안" 챕터가
  이 프로젝트의 실제 구현과 1:1로 맞아떨어져야 하므로 SQL/Postgres여야 한다.
- **콘텐츠는 `src/data/chapters.js` 한 파일에 모은다.** 챕터를 고치거나 추가할 때
  이 파일만 건드린다. `id`가 URL, `order`가 정렬. 나중에 Supabase `chapters`
  테이블로 옮길 때 이 객체 모양을 그대로 스키마로 쓴다.
- **프롬프트는 챕터 안에 심는다.** 별도 "프롬프트 챕터"를 만들지 않는다 —
  기획 프롬프트와 디버깅 프롬프트는 성격이 다르기 때문. `/prompts`는
  각 챕터의 프롬프트를 모아 보여주는 레퍼런스일 뿐 별도 콘텐츠가 아니다.

## 커리큘럼 (8단계)

`00 준비 · 01 기획 · 02 설계 · 03 빌드 · 04 데이터 · 05 보안 · 06 배포 · 07 유지보수`

사용자가 처음 제안한 건 `기획 → 빌드 → DB/보안` 세 단계였다. 앞(도구 세팅·첫 대화),
뒤(배포), 그리고 "막혔을 때 빠져나오는 법"을 더해 8단계가 됐다. 이 셋은 초보가
실제로 무너지는 지점이라 빼지 않는다.

## 디자인 무드

레퍼런스: https://paysages.studio (minimal.gallery 등재)

- 따뜻한 종이 톤 오프화이트 배경 + 숲 그늘 세이지 + 클레이 포인트
- 디스플레이는 명조: `Instrument Serif`(라틴) + `Gowun Batang`(한글), 본문은 `Pretendard`
- 넉넉한 여백, 얇은 1px 구분선, 작은 대문자 레터스페이싱 라벨, 장식 최소
- 토큰은 전부 `src/index.css` 상단 `:root`에 있다. **하드코딩된 색을 새로 쓰지 말고
  변수를 쓴다.** 라이트/다크 둘 다 정의되어 있다.
- 한글 줄바꿈이 어색해지지 않게 텍스트 블록에 `word-break: keep-all`을 유지한다.

## 현재 상태

동작함: 전체 라우팅, 반응형, 라이트/다크, 8챕터 목차 + 단계별 프롬프트(복사 버튼),
진도 체크 → 상단 % / 커리큘럼 / 내 진도 즉시 반영.

**아직 없음:**
- 챕터 본문 — `sections`는 목차 문자열만 있고 내용이 비어 있다 (화면에 "본문 준비 중")
- 로그인 — 상단 버튼은 `/me`로 보내기만 한다
- 서버 진도 저장 — 지금은 localStorage, 기기 바꾸면 초기화
- `/admin` 접근 제어 — 누구나 열 수 있다

## 다음에 할 일 (순서)

1. Supabase 프로젝트 생성 → `.env`에 `VITE_SUPABASE_URL`, `VITE_SUPABASE_ANON_KEY`
2. 테이블 `profiles(id, email, is_admin)`, `progress(user_id, chapter_id, done, updated_at)`
3. 두 테이블 RLS 켜기 — 본인 행만 읽고 쓰기, `is_admin`만 전체 조회
4. `src/lib/progress.js`의 `read()`/`write()`만 Supabase 호출로 교체.
   화면은 전부 `useProgress()`만 쓰므로 **이 파일 하나만 바꾸면 된다.** 이 경계를 깨지 말 것.
5. `/admin`에 관리자 가드
6. 챕터 본문 채우기

## 주의

- `.env`는 `.gitignore`에 있다. 키를 코드에 직접 쓰지 않는다 —
  보안 챕터에서 가르치는 걸 이 저장소가 먼저 지켜야 한다.
- 원격 저장소는 아직 없다. 커밋 1개(`5721a23`)만 있는 로컬 repo.

## 명령

```bash
npm run dev      # http://localhost:5173
npm run build
```
